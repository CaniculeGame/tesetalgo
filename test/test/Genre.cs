using System;
using System.Collections.Generic;
using System.Text;

namespace test
{
    enum Genre
    {
        OWN = 0,
        VISIBLE = 1,
        PAS_VISIBLE =2,
        VISIBLE_REACHPOINT = 3,
        PAS_VISIBLE_REACHPOINT = 4
    }
}
